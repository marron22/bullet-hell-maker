# 開発日誌 v0.25

- 全難易度の Unity 向けデータをまとめて ZIP 書き出しできるようにしました。
- ZIP の中には Easy / Normal / Lunatic ごとの `*.stagedata.json` と `*.bulletbuffers.json` が入ります。
- BulletBuffer の弾サイズを `scale: { x, y }` 形式で出力するようにしました。
- レーザーなど幅と高さのパラメータを持つ攻撃は、Unity 向け出力にもそのサイズ情報を反映します。
